# Local Smoke Rules

Minimal filesystem-only rule document for end-to-end CLI smoke tests.
Every rule routes deterministically to the filesystem backend.

## File Existence

- `README.md` must exist.

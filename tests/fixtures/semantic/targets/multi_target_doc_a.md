# Reference: gate-keeper CLI

The CLI exposes the following commands:

- `gate-keeper validate` — runs every compiled rule against the given target.
- `gate-keeper compile` — turns a Markdown rule document into the canonical IR JSON.
- `gate-keeper diagnose` — prints the resolved provider configuration.

Each command accepts the `--help` flag to print its usage.
